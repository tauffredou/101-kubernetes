apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: clickcount-preprod
spec:
  rules:
  - host: preprod.changeme.xke.techx.fr
    http:
      paths:
      - backend:
          serviceName: clickcount
          servicePort: 8080
